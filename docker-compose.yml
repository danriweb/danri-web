version: "3.9"

services:
  nextjs:
    image: ghcr.io/danriweb/danri-web:latest
    container_name: nextjs
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production